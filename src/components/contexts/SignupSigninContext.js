import { createContext } from 'react';

export const UserDataContext = createContext([]);
export const SigninAccountContext = createContext(null);
export const RespondsDataContext = createContext([]);
